# This is a brief description to help you build kconfig tools

```
cd tools/kconfig-frontends
make clean
./configure --prefix=/usr
make
sudo make install
```
After that, you can try again `make menuconfig`.
